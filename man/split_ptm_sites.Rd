% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_network.R
\name{split_ptm_sites}
\alias{split_ptm_sites}
\title{Split a PTM-site regulator name into gene symbol + site, if present}
\usage{
split_ptm_sites(network, ptm_sep)
}
\arguments{
\item{network}{an edge table with a \code{Regulator} column, or \code{NULL}.}

\item{ptm_sep}{separator to split on (e.g. \code{"."} for \code{SOX2.S35}, \code{"_"} for
\code{MEF2C_S453s}). Only regulators actually containing \code{ptm_sep} are split;
others are left as-is. If none contain it, \code{network} is returned
unchanged (no \code{Site} column added) -- so non-PTM data keeps exactly the
\code{Regulator}/\code{Interaction}/\code{Target}/\code{Weight} schema it always has.}
}
\value{
\code{network} with \code{Regulator} reduced to gene symbols and a \code{Site}
column added (\code{NA} for regulators that had no \code{ptm_sep}), or \code{network}
unchanged if it's \code{NULL}, empty, or no \code{Regulator} contains \code{ptm_sep}.
}
\description{
Regulator names get matched against target gene names elsewhere (hub
connection, network visualization, out-degree counting); a PTM-annotated
regulator like \code{SOX2.S35} would otherwise show up as a node distinct from
the plain-gene-symbol target \code{SOX2}, when they're the same underlying gene.
This strips the site off \code{Regulator} into its own \code{Site} column so
\code{Regulator} is always just the gene symbol.
}
\keyword{internal}
