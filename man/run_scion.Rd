% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_scion.R
\name{run_scion}
\alias{run_scion}
\title{Run SCION: read inputs, cluster once, infer a network}
\usage{
run_scion(
  target_data_file,
  reg_data_file,
  target_genes_file = NULL,
  reg_genes_file = NULL,
  gene_list_header = TRUE,
  format = c("auto", "csv", "gct"),
  clustering_method = c("none", "dtw", "ica", "kmeans", "upload"),
  clustering_data_file = NULL,
  clustering_threshold = 0.5,
  clusters_file = NULL,
  connect_hubs = TRUE,
  weightthreshold = 0,
  normalize = TRUE,
  num.cores = 1,
  ptm_sep = ".",
  seed = 2020,
  permute = FALSE,
  n_permutations = 100,
  permute_dim = c("col", "row"),
  base_seed = 0,
  target_fdr = 0.05,
  output_file = NULL,
  ...
)
}
\arguments{
\item{target_data_file, reg_data_file, target_genes_file, reg_genes_file, }{gene_list_header,format passed to \code{\link[=read_scion_inputs]{read_scion_inputs()}}.}

\item{clustering_method}{passed to \code{\link[=cluster_genes]{cluster_genes()}} as \code{method}: \code{"none"}
(default), \code{"dtw"}, \code{"ica"}, \code{"kmeans"}, or \code{"upload"}.}

\item{clustering_data_file}{path to a clustering matrix, required unless
\code{clustering_method} is \code{"none"} or \code{"upload"}.}

\item{clustering_threshold}{passed to \code{\link[=cluster_genes]{cluster_genes()}} as \code{threshold}.}

\item{clusters_file}{passed to \code{\link[=cluster_genes]{cluster_genes()}} as \code{clusters_file}, required
when \code{clustering_method = "upload"}.}

\item{connect_hubs, num.cores, ptm_sep}{passed to \code{\link[=infer_network]{infer_network()}}.}

\item{weightthreshold}{passed to \code{\link[=infer_network]{infer_network()}}. Forced to \code{0} (with a warning) whenever
\code{permute = TRUE}, regardless of what's passed -- the FDR comparison needs the full,
unthresholded network on both sides; apply a cutoff to the result afterward instead (see
\code{\link[=compute_fdr_threshold]{compute_fdr_threshold()}}).}

\item{normalize}{passed to \code{\link[=infer_network]{infer_network()}}. Forced to \code{FALSE} (with a warning) whenever
\code{permute = TRUE}, regardless of what's passed -- normalizing rescales each network (real
and every permutation) independently to \verb{[0, 1]}, which would force every permutation's
top edge weight to exactly 1 and invalidate the rank-based FDR comparison.}

\item{seed}{RNG seed set once, before clustering and inference, so the same
inputs produce the same network every run. Default matches the legacy
\code{SCION()} behavior. Set to \code{NULL} to skip seeding.}

\item{permute}{if \code{TRUE}, also run \code{\link[=permute_network]{permute_network()}} and
\code{\link[=compute_fdr_threshold]{compute_fdr_threshold()}} against the just-inferred network, in this same
call. Runs \code{n_permutations} permutations using the SAME \code{weightthreshold},
\code{normalize}, \code{connect_hubs}, \code{ptm_sep}, \code{num.cores}, and \code{...}
used for the real network above -- there is no separate way to set these
for the permutations, since the FDR calculation requires them to match.
For sharding permutations across an HPC job array instead of running them
all in this one call, use \code{permute = FALSE} here and call
\code{\link[=permute_network]{permute_network()}} / \code{\link[=save_permutation]{save_permutation()}} / \code{\link[=load_permutations]{load_permutations()}}
directly against this call's \code{target}/\code{reg}/\code{cluster_assignment}.}

\item{n_permutations, permute_dim, base_seed}{passed to \code{\link[=permute_network]{permute_network()}}
when \code{permute = TRUE}.}

\item{target_fdr}{passed to \code{\link[=compute_fdr_threshold]{compute_fdr_threshold()}} when \code{permute = TRUE}.}

\item{output_file}{optional path to write the final edge table to (tab-
separated, Cytoscape-importable). \code{NULL} (default) writes nothing. When
\code{permute = TRUE}, writes the FDR-thresholded network, not the raw one.
Also triggers \code{\link[=save_diagnostic_plots]{save_diagnostic_plots()}}, saved alongside it (same
directory, named from \code{output_file}'s base name) -- there's no Shiny app
to view/download them from interactively in this CLI/scripted path, so
they're written automatically instead of requiring a separate call.}

\item{...}{additional arguments passed to \code{\link[=infer_network]{infer_network()}} /
\code{\link[=RS.Get.Weight.Matrix]{RS.Get.Weight.Matrix()}} (and, when \code{permute = TRUE}, to the internal
\code{\link[=permute_network]{permute_network()}} call as well, so e.g. \code{nb.trees} stays consistent
between the real network and its permutations).}
}
\value{
a list with \code{network} (the real edge table), \code{target}, \code{reg} (the
processed input matrices), \code{cluster_assignment} (or \code{NULL}), \code{params}
(the arguments used, for reference / for feeding into \code{\link[=permute_network]{permute_network()}}
yourself), and -- only when \code{permute = TRUE} -- \code{permuted_networks} (the
\code{\link[=permute_network]{permute_network()}} result), \code{fdr_result} (the \code{\link[=compute_fdr_threshold]{compute_fdr_threshold()}}
result), and \code{network_thresholded} (shorthand for
\code{fdr_result$thresholded_network}).
}
\description{
User-facing wrapper around \code{\link[=read_scion_inputs]{read_scion_inputs()}}, \code{\link[=cluster_genes]{cluster_genes()}}, and
\code{\link[=infer_network]{infer_network()}}. Unlike the legacy \code{SCION()} function, this does not
require a working directory or hardcode file output -- it returns
everything in memory, and optionally writes a Cytoscape-importable edge
table if \code{output_file} is given. The returned list (particularly \code{target},
\code{reg}, and \code{cluster_assignment}) has everything \code{\link[=permute_network]{permute_network()}} needs to
run permutations against this same network without recomputing clustering.
}
