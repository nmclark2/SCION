% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_scion_inputs.R
\name{read_scion_inputs}
\alias{read_scion_inputs}
\title{Read SCION input matrices from CSV or GCT files}
\usage{
read_scion_inputs(
  target_data_file,
  reg_data_file,
  target_genes_file = NULL,
  reg_genes_file = NULL,
  clustering_data_file = NULL,
  format = c("csv", "gct")
)
}
\arguments{
\item{target_data_file, reg_data_file}{path to target/regulator expression
matrices. CSV: first column = gene names, remaining columns = samples.
GCT: a GCT(x) file, read via \code{cmapR::parse_gctx()} (requires the optional
\code{cmapR} package -- install with \code{BiocManager::install("cmapR")}).}

\item{target_genes_file, reg_genes_file}{optional path to a CSV listing which
genes to keep as targets/regulators (first column = gene names). \code{NULL}
(default) keeps every gene present in the corresponding data file.}

\item{clustering_data_file}{optional path to a CSV clustering matrix (rows =
genes, columns = samples), used by \code{\link[=cluster_genes]{cluster_genes()}}. Restricted to genes
that appear in \code{target_genes_file} or \code{reg_genes_file} when those are given.}

\item{format}{\code{"csv"} (default) or \code{"gct"}. Regulator gene names may use
either a dot (\code{SOX2.S35}) or underscore (\code{MEF2C_S453s}) PTM-site
convention; both are left as-is here (see the \code{ptm_sep} argument of
\code{\link[=infer_network]{infer_network()}} for where the convention matters downstream).}
}
\value{
a list with \code{target} and \code{reg} data frames (genes as rows, samples as
columns, row names made syntactically valid via \code{\link[=make.names]{make.names()}}), and
\code{cluster_data} (or \code{NULL} if \code{clustering_data_file} was not given).
}
\description{
Reads target and regulator expression matrices (and, optionally, a separate
clustering matrix), restricts each to a gene list if provided, and drops any
row containing a missing value -- SCION does not support missing values, so
unlike the private-version \code{na.max} proportional filter, this is always an
all-or-nothing drop.
}
