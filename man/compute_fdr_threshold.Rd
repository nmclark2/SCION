% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_fdr_threshold.R
\name{compute_fdr_threshold}
\alias{compute_fdr_threshold}
\title{Compute a permutation-based FDR and edge-weight cutoff}
\usage{
compute_fdr_threshold(real_network, permuted_networks, target_fdr = 0.05)
}
\arguments{
\item{real_network}{the real network's edge table (must have a \code{Weight}
column), e.g. the \code{network} element of \code{\link[=run_scion]{run_scion()}}'s result.}

\item{permuted_networks}{a list of permuted edge tables, as returned by
\code{\link[=permute_network]{permute_network()}}. \strong{Must have been produced with the same \code{engine}} as
\code{real_network} -- weights from different engines are not on comparable
scales, which would invalidate this rank-matching.}

\item{target_fdr}{the FDR threshold to select a weight cutoff at (default 0.05).}
}
\value{
a list:
\item{curve}{a data frame with one row per ranked real-network weight:
\code{weight}, \code{p_value}, \code{fdr} -- the input to \code{\link[=plot_fdr_curve]{plot_fdr_curve()}}.}
\item{threshold}{the chosen edge-weight cutoff, or \code{NA} if no rank achieves
\code{target_fdr}.}
\item{target_fdr}{echoes the input.}
\item{thresholded_network}{\code{real_network} filtered to \code{Weight >= threshold}.}
\item{permuted_weights}{the rank x permutation matrix of (padded) permuted
weights, useful for diagnostic plots.}
}
\description{
Ports the validated permutation-testing procedure exactly: for each rank
position in the sorted, nonzero real network weights, computes an empirical
p-value from how often permuted networks exceed the real weight at that
rank, BH-adjusts across ranks, and picks the smallest real weight for which
FDR is still below \code{target_fdr}.
}
