% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_run.R
\name{info_tooltip}
\alias{info_tooltip}
\title{A small "(i)" icon that shows \code{...} (pasted together) as a hover tooltip --
for a label that needs a sentence of explanation without permanently taking
up sidebar space the way a \code{helpText()} paragraph would. Pure CSS (see the
\code{.scion-tooltip} rule in \code{\link[=app_UI]{app_UI()}}) rather than the native \code{title}
attribute, which is inconsistent across browsers (slow to appear, or not at
all for inline elements in some layouts).}
\usage{
info_tooltip(...)
}
\description{
A small "(i)" icon that shows \code{...} (pasted together) as a hover tooltip --
for a label that needs a sentence of explanation without permanently taking
up sidebar space the way a \code{helpText()} paragraph would. Pure CSS (see the
\code{.scion-tooltip} rule in \code{\link[=app_UI]{app_UI()}}) rather than the native \code{title}
attribute, which is inconsistent across browsers (slow to appear, or not at
all for inline elements in some layouts).
}
\keyword{internal}
