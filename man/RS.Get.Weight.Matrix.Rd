% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RS.Get.Weight.Matrix.R
\name{RS.Get.Weight.Matrix}
\alias{RS.Get.Weight.Matrix}
\title{Infer a weighted regulator -> target network via random-forest importance}
\usage{
RS.Get.Weight.Matrix(
  target.matrix,
  input.matrix,
  K = "sqrt",
  nb.trees = 10000,
  importance.measure = "\%IncMSE",
  seed = NULL,
  trace = TRUE,
  normalize = TRUE,
  num.cores = 1,
  ...
)
}
\arguments{
\item{target.matrix}{data frame/matrix of target expression, samples as rows,
genes as columns.}

\item{input.matrix}{data frame/matrix of regulator (input) expression, samples
as rows, genes as columns.}

\item{K}{number of candidate regulators considered at each tree split: \code{"sqrt"},
\code{"all"}, or an integer.}

\item{nb.trees}{number of trees per random forest.}

\item{importance.measure}{\code{"IncNodePurity"} or \code{"\%IncMSE"}.}

\item{seed}{optional RNG seed for the top-level per-target seed draw. Must be
set consistently between a real network and its permutations for the
permutation seeding scheme in \code{\link[=permute_network]{permute_network()}} to be reproducible.}

\item{trace}{if \code{TRUE}, emit a progress message per target gene.}

\item{normalize}{if \code{TRUE}, rescale the weight matrix to \verb{[0, 1]}.}

\item{num.cores}{number of cores to use: a \code{num.cores - 1} FORK cluster is
used to parallelize across target genes (disabled when \code{num.cores <= 2}).}

\item{...}{additional arguments passed to \code{randomForest::randomForest()}.}
}
\value{
a numeric matrix of edge weights (targets x regulators), or \code{NULL} if
there are no targets or no regulators.
}
\description{
For each target gene, fits a random forest predicting its expression from all
regulator features, and records each regulator's importance as the edge
weight. This is the GENIE3-style core of SCION's network inference, and is
called identically for the real network and for every permutation generated
by \code{\link[=permute_network]{permute_network()}}.
}
