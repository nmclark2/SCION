% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permute_network.R
\name{permute_network}
\alias{permute_network}
\title{Generate permuted null networks for FDR thresholding}
\usage{
permute_network(
  target,
  reg,
  cluster_assignment = NULL,
  n_permutations = 100,
  indices = seq_len(n_permutations),
  permute_dim = c("col", "row"),
  base_seed = 0,
  num.cores = 1,
  weightthreshold = 0,
  normalize = TRUE,
  connect_hubs = TRUE,
  ptm_sep = ".",
  ...
)
}
\arguments{
\item{target, reg}{the processed target/regulator matrices used for the real
network (the \code{target}/\code{reg} elements of \code{\link[=run_scion]{run_scion()}}'s result).}

\item{cluster_assignment}{the fixed cluster assignment used for the real network
(the \code{cluster_assignment} element of \code{\link[=run_scion]{run_scion()}}'s result, or \code{NULL}).}

\item{n_permutations}{number of permutations (typically 100). Ignored if
\code{indices} is given explicitly.}

\item{indices}{which permutation indices to run; each index \code{i} is seeded
with \code{base_seed + i}, independent of every other index and of \code{num.cores}
(see Details). Defaults to \code{seq_len(n_permutations)}, i.e. all of them in
one call. Pass a single index -- e.g. an HPC job array's task ID -- to run
just one permutation per job; see \code{\link[=save_permutation]{save_permutation()}}/\code{\link[=load_permutations]{load_permutations()}}
for a file-based workflow built around exactly that pattern.}

\item{permute_dim}{passed to \code{\link[=shuffle_matrix]{shuffle_matrix()}} as \code{dim}; \code{"col"} (default)
matches the validated lab scheme.}

\item{base_seed}{permutation \code{i} uses seed \code{base_seed + i}. Default 0
reproduces the lab scheme (\code{set.seed(i)}).}

\item{num.cores}{when \verb{> 2}, the requested indices run across a FORK cluster
(one permutation per worker at a time); each permutation's own
\code{\link[=infer_network]{infer_network()}} call is then forced to \code{num.cores = 1} internally to
avoid nesting parallelism. When \code{num.cores <= 2}, indices run serially (in
this process or, for a single index, as whatever single HPC job called
this) and their own \code{num.cores} is passed straight through to each
\code{\link[=infer_network]{infer_network()}} call.}

\item{weightthreshold}{passed to \code{\link[=infer_network]{infer_network()}}. \strong{Must match whatever was used for the
real network} (see \code{normalize} below), and should generally be \code{0}: the FDR comparison in
\code{\link[=compute_fdr_threshold]{compute_fdr_threshold()}} needs the full, unthresholded network on both sides -- a nonzero
value applies the same manual cutoff before that comparison ever happens, biasing which
edges get compared (see \code{\link[=run_scion]{run_scion()}}, which enforces this automatically when calling this
function with \code{permute = TRUE}).}

\item{normalize}{passed to \code{\link[=infer_network]{infer_network()}}. \strong{Must match whatever was used for the real
network} this permutation set will be compared against in \code{\link[=compute_fdr_threshold]{compute_fdr_threshold()}}, and
should generally be \code{FALSE} for both: normalizing rescales each network independently to
\verb{[0, 1]}, forcing every permutation's top edge weight to exactly 1 regardless of its
actual signal, which invalidates the rank-based FDR comparison (see \code{\link[=run_scion]{run_scion()}}, which
enforces this automatically when calling this function with \code{permute = TRUE}).}

\item{connect_hubs, ptm_sep}{passed to \code{\link[=infer_network]{infer_network()}}.}

\item{...}{additional arguments passed to \code{\link[=infer_network]{infer_network()}}.}
}
\value{
a list the same length as \code{indices}, each element an edge table as
returned by \code{\link[=infer_network]{infer_network()}}, named by its permutation index (as a string).
}
\description{
Shuffles the target/regulator matrices and reruns \code{\link[=infer_network]{infer_network()}} \code{n_permutations}
times, reusing the SAME fixed \code{cluster_assignment} every time (clustering is never
recomputed per permutation -- see \code{\link[=cluster_genes]{cluster_genes()}}). Ported from the validated
lab procedure (\code{scion_data_processing.R}): each permutation \code{i} is seeded with
\code{base_seed + i} before shuffling, which makes results independent of \code{num.cores}
for the same reason the per-target-gene seeding in \code{\link[=RS.Get.Weight.Matrix]{RS.Get.Weight.Matrix()}} does.
}
\details{
Emits a \code{"SCION_STAGE: permutation testing progress <done>/<total>"} message
after every completed permutation (serial path) or every completed batch of
\code{num.cores - 1} permutations (parallel path -- a single \code{parLapply()} call
blocks until it returns, so there's no way to observe progress \emph{within} one
call; splitting the work into per-worker-sized batches trades a little
dispatch overhead for a progress update roughly every \code{num.cores - 1}
permutations). The Shiny app's Run tab listens for these to drive its
progress bar smoothly across the whole permutation phase instead of sitting
still until it's all done.
}
