% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{hover_line}
\alias{hover_line}
\title{A dashed reference line that's hoverable along its entire length, not just its endpoints}
\usage{
hover_line(p, orientation = c("vertical", "horizontal"), at, text, n = 100)
}
\arguments{
\item{p}{a \code{ggplot2} object, with its data/scale layers already added (this function reads
their actual rendered range to span the line fully, so call it last).}

\item{orientation}{\code{"vertical"} (constant x, spanning the panel's y range) or \code{"horizontal"}
(constant y, spanning the panel's x range).}

\item{at}{the x (if vertical) or y (if horizontal) position of the line.}

\item{text}{hover text for the line.}

\item{n}{number of points along the line (100 is dense enough that no gap is perceptible).}
}
\value{
\code{p} with the reference line layer added.
}
\description{
\code{ggplot2::geom_vline()}/\code{geom_hline()} are drawn as a single 2-point segment, so
\code{plotly::ggplotly()}'s hover (which snaps to the nearest data point on a line trace) only
triggers near the two endpoints -- everywhere else along the line, nothing happens. Using many
points along the same segment instead means there's always a nearby point to snap to, so hover
works no matter where along the line the cursor is.
}
\keyword{internal}
