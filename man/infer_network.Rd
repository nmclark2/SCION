% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_network.R
\name{infer_network}
\alias{infer_network}
\title{Infer a (possibly clustered) network from target/regulator matrices}
\usage{
infer_network(
  target_data,
  reg_data,
  cluster_assignment = NULL,
  weightthreshold = 0,
  normalize = TRUE,
  connect_hubs = TRUE,
  num.cores = 1,
  engine = c("randomForest", "ranger"),
  ptm_sep = ".",
  seed = NULL,
  ...
)
}
\arguments{
\item{target_data}{data frame/matrix, genes as rows, samples as columns.}

\item{reg_data}{data frame/matrix, genes as rows, samples as columns.}

\item{cluster_assignment}{optional data frame with a \code{clusters} column (row
names = gene names), as returned by \code{\link[=cluster_genes]{cluster_genes()}}. \code{NULL} means infer a
single network across all genes.}

\item{weightthreshold}{edges with weight below this value are dropped.}

\item{normalize}{passed to \code{\link[=RS.Get.Weight.Matrix]{RS.Get.Weight.Matrix()}}.}

\item{connect_hubs}{if clustering, whether to additionally infer a network
connecting each cluster's hub gene (highest out-degree regulator).}

\item{num.cores}{passed to \code{\link[=RS.Get.Weight.Matrix]{RS.Get.Weight.Matrix()}}.}

\item{engine}{passed to \code{\link[=RS.Get.Weight.Matrix]{RS.Get.Weight.Matrix()}}. Must match whatever engine
produced any network this one will be compared against (see
\code{\link[=RS.Get.Weight.Matrix]{RS.Get.Weight.Matrix()}} and \code{\link[=compute_fdr_threshold]{compute_fdr_threshold()}}).}

\item{ptm_sep}{separator used to split a PTM-site regulator name into gene
symbol + site (e.g. \code{"."} for \code{SOX2.S35}, \code{"_"} for \code{MEF2C_S453s}): when
connecting cluster hubs and no hub gene is directly present in
\code{target_data}'s row names, and always in the returned edge table (see
\code{\link[=split_ptm_sites]{split_ptm_sites()}}) so a PTM-annotated regulator and its plain-symbol
target are recognized as the same gene.}

\item{seed}{optional RNG seed set once, at the very start of this call
(before clustering-aware seed draws or any inference). Without it, the
result depends on the ambient global RNG state at call time -- pass an
explicit \code{seed} (or call \code{set.seed()} yourself right before calling) any
time you need two calls to be comparable/reproducible.}

\item{...}{additional arguments passed to \code{\link[=RS.Get.Weight.Matrix]{RS.Get.Weight.Matrix()}}.}
}
\value{
a data frame with columns \code{Regulator}, \code{Interaction}, \code{Target}, \code{Weight},
or \code{NULL} if inference could not be run (e.g. no targets/regulators).
}
\description{
The core network-inference step of SCION, with no file I/O: given target and
regulator expression matrices (genes as rows, samples as columns) and an
optional, already-computed cluster assignment, infers one network per
cluster (or a single network if \code{cluster_assignment} is \code{NULL}), optionally
connects cluster hubs, and returns a single edge table. This function is
called identically by \code{\link[=run_scion]{run_scion()}} for the real network and by
\code{\link[=permute_network]{permute_network()}} for every permutation -- it never recomputes clustering.
}
