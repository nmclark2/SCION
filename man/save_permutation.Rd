% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation_io.R
\name{save_permutation}
\alias{save_permutation}
\title{Save one permutation's network to disk}
\usage{
save_permutation(network, index, dir = ".", prefix = "permutation")
}
\arguments{
\item{network}{a single permuted network (edge table), e.g.
\code{permute_network(..., indices = task_id)[[1]]}.}

\item{index}{the permutation index this network corresponds to (e.g. the
HPC job array task ID).}

\item{dir}{directory to save into (created if it doesn't exist).}

\item{prefix}{filename prefix; the file is saved as \verb{<prefix>_<index>.rds}.}
}
\value{
the path written to, invisibly.
}
\description{
Companion to \code{\link[=load_permutations]{load_permutations()}} for an HPC job-array workflow: each array
task runs one permutation (via \code{permute_network(..., indices = task_id)})
and saves its result with this function; a final job reads them all back
with \code{\link[=load_permutations]{load_permutations()}} and passes them to \code{\link[=compute_fdr_threshold]{compute_fdr_threshold()}}.
}
