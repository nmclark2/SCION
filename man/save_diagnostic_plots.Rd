% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_diagnostic_plots.R
\name{save_diagnostic_plots}
\alias{save_diagnostic_plots}
\title{Save SCION's diagnostic plots as publication-quality PDFs}
\usage{
save_diagnostic_plots(
  result,
  dir,
  prefix = "diagnostics",
  width = 7,
  height = 5,
  units = "in"
)
}
\arguments{
\item{result}{a \code{\link[=run_scion]{run_scion()}} result list.}

\item{dir}{directory to save into (created, recursively, if it doesn't exist).}

\item{prefix}{filename prefix for each saved plot.}

\item{width, height, units}{passed to \code{ggplot2::ggsave()}.}
}
\value{
invisibly, a character vector of the file paths written.
}
\description{
Writes the edge weight distribution and regulator out-degree distribution
(and, if permutations were run, the FDR curve and real-vs-permuted weight
comparison) as separate single-plot PDFs via \code{ggplot2::ggsave()} -- vector
output, so resolution is never a concern. This is what \code{\link[=run_scion]{run_scion()}} calls
automatically whenever \code{output_file} is set (i.e. CLI/scripted use, where
there's no app to view the interactive versions in); call it directly for
any other \code{run_scion()} result you want plots from.
}
